
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    struct Pointers {
        int pointer1[5] = {};   // Allow for 5 pointers max
            p1  = *pointer1;    // Declares a pointer to array pointer1
            p2  = &p1;          // Assigns the pointer to p2
        
        int memory_allocation [] = {}
            x = *memory_allocation
            y = &x
        
        char pointer_name [] = "Name goes here...";
        
        
    };
    
    /* Point to that instance */
    struct Pointers new_instance;
    
    // Read and display a list of files in the current directory
    file_pointer = fopen()
    
    // Ability to navigate between directories within context of program
    
    return 0;
}
